[tool.ruff]
target-version = "py310"
exclude = [
    ".git",
    "venv",
    ".venv",
    "__pycache__",
]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["config", "scripts", "utils", "models"]
