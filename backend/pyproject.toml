[tool.ruff]
target-version = "py310"
exclude = [
    ".git",
    "venv",
    ".venv",
    "__pycache__",
]
line-length = 120
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["config", "scripts", "utils", "models"]
